///
/// JContact.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "Contact.hpp"

#include "EmailAddress.hpp"
#include "JEmailAddress.hpp"
#include "JPhoneNumber.hpp"
#include "PhoneNumber.hpp"
#include <optional>
#include <string>
#include <vector>

namespace margelo::nitro::margeloContact {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "Contact" and the the Kotlin data class "Contact".
   */
  struct JContact final: public jni::JavaClass<JContact> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/margeloContact/Contact;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct Contact by copying all values to C++.
     */
    [[maybe_unused]]
    Contact toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldFirstName = clazz->getField<jni::JString>("firstName");
      jni::local_ref<jni::JString> firstName = this->getFieldValue(fieldFirstName);
      static const auto fieldLastName = clazz->getField<jni::JString>("lastName");
      jni::local_ref<jni::JString> lastName = this->getFieldValue(fieldLastName);
      static const auto fieldPhoneNumbers = clazz->getField<jni::JArrayClass<JPhoneNumber>>("phoneNumbers");
      jni::local_ref<jni::JArrayClass<JPhoneNumber>> phoneNumbers = this->getFieldValue(fieldPhoneNumbers);
      static const auto fieldEmailAddresses = clazz->getField<jni::JArrayClass<JEmailAddress>>("emailAddresses");
      jni::local_ref<jni::JArrayClass<JEmailAddress>> emailAddresses = this->getFieldValue(fieldEmailAddresses);
      static const auto fieldThumbnail = clazz->getField<jni::JString>("thumbnail");
      jni::local_ref<jni::JString> thumbnail = this->getFieldValue(fieldThumbnail);
      return Contact(
        firstName != nullptr ? std::make_optional(firstName->toStdString()) : std::nullopt,
        lastName != nullptr ? std::make_optional(lastName->toStdString()) : std::nullopt,
        phoneNumbers != nullptr ? std::make_optional([&]() {
          size_t size = phoneNumbers->size();
          std::vector<PhoneNumber> vector;
          vector.reserve(size);
          for (size_t i = 0; i < size; i++) {
            auto element = phoneNumbers->getElement(i);
            vector.push_back(element->toCpp());
          }
          return vector;
        }()) : std::nullopt,
        emailAddresses != nullptr ? std::make_optional([&]() {
          size_t size = emailAddresses->size();
          std::vector<EmailAddress> vector;
          vector.reserve(size);
          for (size_t i = 0; i < size; i++) {
            auto element = emailAddresses->getElement(i);
            vector.push_back(element->toCpp());
          }
          return vector;
        }()) : std::nullopt,
        thumbnail != nullptr ? std::make_optional(thumbnail->toStdString()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JContact::javaobject> fromCpp(const Contact& value) {
      return newInstance(
        value.firstName.has_value() ? jni::make_jstring(value.firstName.value()) : nullptr,
        value.lastName.has_value() ? jni::make_jstring(value.lastName.value()) : nullptr,
        value.phoneNumbers.has_value() ? [&]() {
          size_t size = value.phoneNumbers.value().size();
          jni::local_ref<jni::JArrayClass<JPhoneNumber>> array = jni::JArrayClass<JPhoneNumber>::newArray(size);
          for (size_t i = 0; i < size; i++) {
            const auto& element = value.phoneNumbers.value()[i];
            array->setElement(i, *JPhoneNumber::fromCpp(element));
          }
          return array;
        }() : nullptr,
        value.emailAddresses.has_value() ? [&]() {
          size_t size = value.emailAddresses.value().size();
          jni::local_ref<jni::JArrayClass<JEmailAddress>> array = jni::JArrayClass<JEmailAddress>::newArray(size);
          for (size_t i = 0; i < size; i++) {
            const auto& element = value.emailAddresses.value()[i];
            array->setElement(i, *JEmailAddress::fromCpp(element));
          }
          return array;
        }() : nullptr,
        value.thumbnail.has_value() ? jni::make_jstring(value.thumbnail.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::margeloContact
