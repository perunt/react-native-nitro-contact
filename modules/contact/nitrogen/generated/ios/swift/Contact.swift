///
/// Contact.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `Contact`, backed by a C++ struct.
 */
public typealias Contact = margelo.nitro.margeloContact.Contact

public extension Contact {
  private typealias bridge = margelo.nitro.margeloContact.bridge.swift

  /**
   * Create a new instance of `Contact`.
   */
  init(firstName: String?, lastName: String?, phoneNumbers: [PhoneNumber]?, emailAddresses: [EmailAddress]?, thumbnail: String?) {
    self.init({ () -> bridge.std__optional_std__string_ in
      if let actualValue = firstName {
        return bridge.create_std__optional_std__string_(std.string(actualValue))
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_std__string_ in
      if let actualValue = lastName {
        return bridge.create_std__optional_std__string_(std.string(actualValue))
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_std__vector_PhoneNumber__ in
      if let actualValue = phoneNumbers {
        return bridge.create_std__optional_std__vector_PhoneNumber__({ () -> bridge.std__vector_PhoneNumber_ in
          var vector = bridge.create_std__vector_PhoneNumber_(actualValue.count)
          for item in actualValue {
            vector.push_back(item)
          }
          return vector
        }())
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_std__vector_EmailAddress__ in
      if let actualValue = emailAddresses {
        return bridge.create_std__optional_std__vector_EmailAddress__({ () -> bridge.std__vector_EmailAddress_ in
          var vector = bridge.create_std__vector_EmailAddress_(actualValue.count)
          for item in actualValue {
            vector.push_back(item)
          }
          return vector
        }())
      } else {
        return .init()
      }
    }(), { () -> bridge.std__optional_std__string_ in
      if let actualValue = thumbnail {
        return bridge.create_std__optional_std__string_(std.string(actualValue))
      } else {
        return .init()
      }
    }())
  }

  var firstName: String? {
    @inline(__always)
    get {
      return { () -> String? in
        if let actualValue = self.__firstName.value {
          return String(actualValue)
        } else {
          return nil
        }
      }()
    }
    @inline(__always)
    set {
      self.__firstName = { () -> bridge.std__optional_std__string_ in
        if let actualValue = newValue {
          return bridge.create_std__optional_std__string_(std.string(actualValue))
        } else {
          return .init()
        }
      }()
    }
  }
  
  var lastName: String? {
    @inline(__always)
    get {
      return { () -> String? in
        if let actualValue = self.__lastName.value {
          return String(actualValue)
        } else {
          return nil
        }
      }()
    }
    @inline(__always)
    set {
      self.__lastName = { () -> bridge.std__optional_std__string_ in
        if let actualValue = newValue {
          return bridge.create_std__optional_std__string_(std.string(actualValue))
        } else {
          return .init()
        }
      }()
    }
  }
  
  var phoneNumbers: [PhoneNumber]? {
    @inline(__always)
    get {
      return { () -> [PhoneNumber]? in
        if let actualValue = self.__phoneNumbers.value {
          return actualValue.map({ val in val })
        } else {
          return nil
        }
      }()
    }
    @inline(__always)
    set {
      self.__phoneNumbers = { () -> bridge.std__optional_std__vector_PhoneNumber__ in
        if let actualValue = newValue {
          return bridge.create_std__optional_std__vector_PhoneNumber__({ () -> bridge.std__vector_PhoneNumber_ in
            var vector = bridge.create_std__vector_PhoneNumber_(actualValue.count)
            for item in actualValue {
              vector.push_back(item)
            }
            return vector
          }())
        } else {
          return .init()
        }
      }()
    }
  }
  
  var emailAddresses: [EmailAddress]? {
    @inline(__always)
    get {
      return { () -> [EmailAddress]? in
        if let actualValue = self.__emailAddresses.value {
          return actualValue.map({ val in val })
        } else {
          return nil
        }
      }()
    }
    @inline(__always)
    set {
      self.__emailAddresses = { () -> bridge.std__optional_std__vector_EmailAddress__ in
        if let actualValue = newValue {
          return bridge.create_std__optional_std__vector_EmailAddress__({ () -> bridge.std__vector_EmailAddress_ in
            var vector = bridge.create_std__vector_EmailAddress_(actualValue.count)
            for item in actualValue {
              vector.push_back(item)
            }
            return vector
          }())
        } else {
          return .init()
        }
      }()
    }
  }
  
  var thumbnail: String? {
    @inline(__always)
    get {
      return { () -> String? in
        if let actualValue = self.__thumbnail.value {
          return String(actualValue)
        } else {
          return nil
        }
      }()
    }
    @inline(__always)
    set {
      self.__thumbnail = { () -> bridge.std__optional_std__string_ in
        if let actualValue = newValue {
          return bridge.create_std__optional_std__string_(std.string(actualValue))
        } else {
          return .init()
        }
      }()
    }
  }
}
